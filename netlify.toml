# Sorgt dafuer, dass die .ics als echter Kalender-Feed ausgeliefert wird
[[headers]]
  for = "/nq-calendar.ics"
  [headers.values]
    Content-Type = "text/calendar; charset=utf-8"
    Cache-Control = "public, max-age=3600"
